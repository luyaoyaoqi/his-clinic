# 辅助诊疗系统—接口对接文档

> 应用系统：辅助诊疗系统（mhi_back_cis）

## 1. 接口概述

### 1.1 接口清单

| 接口编码 | 接口名称 | 方向 | 说明 |
| --- | --- | --- | --- |
| his_outpatient_visit | HIS发起门诊 | inbound | HIS调用平台发起门诊请求 |
| medical_record_query | HIS主动查询病历 | inbound | HIS凭 his_record_id 主动查询平台已同步的病历详情（含诊断、处方） |
| sync_medical_record | 病历数据同步推送 | outbound | 病历保存后推送给 HIS |
| drug_stock_query | 药品库存查询 | outbound | 批量查询药品库存信息 |
| outer_diagnoses_query | 外部诊断信息查询 | outbound | 查询患者四诊（面/舌/舌下/口气/脉）诊断记录，对接外部平台 |

### 1.2 数据流向

- `his_outpatient_visit` — inbound —> 平台（HIS发起门诊请求）
- `medical_record_query` — inbound —> 平台（HIS凭his_record_id查询病历详情）
- `sync_medical_record` — outbound —> HIS系统（接收病历数据）
- `drug_stock_query` — outbound —> HIS系统（查询药品库存）
- `outer_diagnoses_query` — outbound —> 外部诊断设备平台（查询四诊诊断数据）

---

## 2. HIS发起门诊（his_outpatient_visit）

### 2.1 请求地址

HIS通过以下地址调用平台发起门诊请求：

```
POST /api/system/external-api/inbound/<hospital_code>/his_outpatient_visit/
```

该接口为入站接口，由HIS端主动调用，平台接收后处理门诊发起逻辑。

### 2.2 请求方式

| 属性 | 值 |
| --- | --- |
| HTTP Method | POST |
| Content-Type | application/json |
| 请求格式 | JSON |
| 响应格式 | JSON |
| 超时时间 | 30s |
| 重试次数 | 0 |

### 2.3 请求头

| Header | 值 | 必填 | 说明 |
| --- | --- | --- | --- |
| Content-Type | application/json | 是 | 请求体格式 |

### 2.4 请求参数

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| app_id | string | 是 | 医院 AppId |
| app_secret | string | 是 | 医院 AppSecret |
| previous_his_record_id | string | 否 | 上一次 HIS 病历ID |
| his_record_id | string | 是 | HIS 病历唯一编号 |
| institution | object | 是 | 机构信息 |
| department | object | 是 | 科室信息 |
| doctor | object | 是 | 医生信息 |
| patient | object | 是 | 患者信息 |
| record | object | 是 | 病历主信息 |

**institution（机构信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_institution_code | string | 是 | 机构编码 |

**department（科室信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_department_code | string | 是 | 科室编码 |
| name | string | 否 | 科室名称 |

**doctor（医生信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_doctor_code | string | 是 | 医生编码 |
| name | string | 否 | 医生姓名 |

**patient（患者信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| his_patient_id | string | 是 | HIS患者ID |
| name | string | 是 | 姓名 |
| gender | string | 是 | 性别（男/女） |
| age | integer | 是 | 年龄 |
| mobile | string | 否 | 手机号 |
| idcard | string | 否 | 身份证号 |
| sscid | string | 否 | 社保卡号 |
| province | string | 否 | 省份 |
| city | string | 否 | 城市 |
| area | string | 否 | 区域 |
| family_address | string | 否 | 家庭地址 |
| contacts_name | string | 否 | 联系人姓名 |
| contacts_mobile | string | 否 | 联系人手机 |

**record（病历主信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| appointment_date | string | 否 | 就诊日期 |
| his_appointment_no | string | 否 | 就诊单号 |
| his_register_no | string | 否 | HIS预约号 |
| patient_basic | object | 否 | 患者基础信息（见下表） |
| diagnose | object | 否 | 诊断信息（见下表） |

**record.patient_basic（患者基础信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| main_remark | string | 否 | 主诉 |
| input_remark | string | 否 | 现病史 |
| objective_remark | string | 否 | 体格检查 |
| assist_check | string | 否 | 辅助检查 |
| disease_history | string | 否 | 既往史 |
| allergy_history | string | 否 | 过敏史 |
| birth_history | string | 否 | 出生史 |
| personal_history | string | 否 | 个人史 |
| family_history | string | 否 | 家庭史 |
| marital_reproductive_history | string | 否 | 婚育史 |
| epidemics_history | string | 否 | 流行病史 |
| look_listen | string | 否 | 望闻切诊 |
| dialectic | string | 否 | 辨证论治 |
| height | integer | 否 | 身高 |
| weight | integer | 否 | 体重 |
| temperature | float | 否 | 体温 |
| heart_rate | float | 否 | 心率 |
| systolic_blood_pressure | float | 否 | 收缩压 |
| diastolic_blood_pressure | float | 否 | 舒张压 |

**record.diagnose（诊断信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| gbs | array | 否 | 中医诊断(GBS), 每项含 name + code + aliases (别名数组) |
| icd10s | array | 否 | ICD10诊断, 每项含 name + code |
| syndrome | object | 否 | 证型, 含 name + code + aliases (别名数组) |
| syndromes | array | 否 | 证型列表, 每项含 name + code + aliases (别名数组) |
| therapy | object | 否 | 治法, 含 name + code + aliases (别名数组) |
| therapies | array | 否 | 治法列表, 每项含 name + code + aliases (别名数组) |

### 2.5 响应字段

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | 系统返回码（20000=成功） |
| success | boolean | 是否成功 |
| msg | string | 提示信息 |
| data | object | 业务数据容器 |
| data.code | string | 门诊唯一编码 |
| reason | string/null | 失败原因（成功时为null） |
| log_id | string | 平台日志ID |

> 成功判定规则：`$.code eq 20000`

### 2.6 标准对接示例

**请求：**

```bash
curl --location 'localhost:9100/api/system/external-api/inbound/hospitalA/his_outpatient_visit' \
--header 'Content-Type: application/json' \
--data '{
    "app_id": "b673ced0db0faf493649fe7c6c4b41c",
    "app_secret": "X5bYMxyK3neJ-f25sSgKUGUJqIM8Usr9uY1iZ0Wg",
    "previous_his_record_id": "",
    "his_record_id": "34997589",
    "institution": {
        "outer_institution_code": "hospitalA"
    },
    "department": {
        "outer_department_code": "nk_02",
        "name": "内科2"
    },
    "doctor": {
        "outer_doctor_code": "doctor001",
        "name": "医生"
    },
    "patient": {
        "his_patient_id": "DD1577603E1D46DBBF84986D6B950DCE",
        "name": "余喆",
        "gender": "女",
        "age": 32,
        "mobile": "15707647692",
        "idcard": "330825199209161824",
        "sscid": "33080099000000000001873545"
    },
    "record": {
        "appointment_date": "2024-09-18",
        "his_appointment_no": "",
        "his_register_no": "0000535164",
        "patient_basic": {
            "main_remark": "上腹部胀满不适头晕半天。",
            "input_remark": "上腹部胀满不适头晕半天，余无不适",
            "objective_remark": "神志清，精神软，颈软，无抵抗",
            "assist_check": "无特殊",
            "disease_history": "否认高血压、糖尿病病史"
        },
        "diagnose": {
            "icd10s": [
                {"code": "K29.100x001", "name": "急性胃炎"},
                {"code": "A49.809", "name": "幽门螺杆菌感染"}
            ],
            "gbs": [],
            "syndrome": {"code": "", "name": ""},
            "therapy": {"code": "", "name": ""}
        }
    }
}'
```

**响应：**

```json
{
    "code": 20000,
    "success": true,
    "msg": "成功",
    "data": {
        "code": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
    },
    "reason": null,
    "log_id": "f8ae9faadcd442f78ce36de6ebef4e2b"
}
```

---

## 3. 病历数据同步推送（sync_medical_record）

### 3.1 请求地址

#### 3.1.1 HIS 端接收地址（由医院提供）

病历数据同步推送的目标地址为 HIS 端 API 地址，由医院提供。

| 对接模式 | HIS 端地址 | 说明 |
| --- | --- | --- |
| 标准对接 | 由医院提供，配置于系统级 api_url_template | 如 https://his.hospital.com/api/medical-record/sync |
| 自定义对接 | 由医院提供，配置于医院级 api_url | 覆盖系统级模板，指向医院实际 HIS 接口 |

> **重要：** 医院在对接前需提供 HIS 端接收病历数据的 API 地址，由平台管理员在系统配置中录入。

#### 3.1.2 平台触发地址（平台内部调用）

平台通过以下内部 API 触发出站调用，将病历数据推送到医院提供的 HIS 端地址：

```
POST /api/system/external-api/invoke
```

> 触发地址为平台内部 API，由辅诊系统在病历保存后自动调用，HIS 对接方无需关注。

### 3.2 请求方式

| 属性 | 值 |
| --- | --- |
| HTTP Method | POST |
| Content-Type | application/json |
| Authorization | Bearer <token> |
| 请求格式 | JSON |
| 响应格式 | JSON |
| 超时时间 | 30s（标准）/60s（自定义） |
| 重试次数 | 1次 |

### 3.3 请求头

| Header | 值 | 必填 | 说明 |
| --- | --- | --- | --- |
| Content-Type | application/json | 是 | 请求体格式 |
| Authorization | Bearer <token> | 是 | 认证令牌 |

### 3.4 请求参数

> 以下为平台发送至HIS接收地址的业务参数：api_code、params等平台内部调用封装参数不在对接范围内。

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| his_record_id | string | 是 | HIS 病历唯一编号 |
| institution | object | 是 | 机构信息 |
| department | object | 是 | 科室信息 |
| doctor | object | 是 | 医生信息 |
| patient | object | 是 | 患者信息 |
| record | object | 是 | 病历主信息 |

**institution（机构信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_institution_code | string | 是 | 机构编码 |

**department（科室信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_department_code | string | 是 | 科室编码 |
| name | string | 否 | 科室名称 |

**doctor（医生信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| outer_doctor_code | string | 是 | 医生编码 |
| name | string | 否 | 医生姓名 |

**patient（患者信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| his_patient_id | string | 是 | HIS患者ID |
| name | string | 是 | 患者姓名 |
| gender | string | 否 | 性别 |
| age | integer | 否 | 年龄 |
| mobile | string | 否 | 手机号 |
| idcard | string | 否 | 身份证号 |
| sscid | string | 否 | 社保卡号 |
| province | string | 否 | 省份 |
| city | string | 否 | 城市 |
| area | string | 否 | 区域 |
| family_address | string | 否 | 家庭住址 |
| contacts_name | string | 否 | 联系人姓名 |
| contacts_mobile | string | 否 | 联系人手机号 |

**record（病历主信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| appointment_date | string | 否 | 就诊日期 |
| his_appointment_no | string | 否 | 就诊单号 |
| his_register_no | string | 否 | HIS预约号 |
| patient_basic | object | 否 | 患者基础信息（见下表） |
| diagnose | object | 否 | 诊断信息（见下表） |
| tcm_prescription | array | 否 | 中药处方列表（见下表） |
| cpm_prescription | array | 否 | 中成药处方列表（见下表） |
| treatment | array | 否 | 治疗信息列表（适宜技术，见下表） |
| note | string | 否 | 备注 |
| invitation_date | string | 否 | 邀约日期 |

**record.patient_basic（患者基础信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| height | integer | 否 | 身高 |
| weight | integer | 否 | 体重 |
| temperature | float | 否 | 体温 |
| heart_rate | float | 否 | 心率 |
| systolic_blood_pressure | float | 否 | 收缩压 |
| diastolic_blood_pressure | float | 否 | 舒张压 |
| main_remark | string | 否 | 主诉 |
| input_remark | string | 否 | 现病史 |
| objective_remark | string | 否 | 体格检查 |
| assist_check | string | 否 | 辅助检查 |
| disease_history | string | 否 | 既往史 |
| allergy_history | string | 否 | 过敏史 |
| birth_history | string | 否 | 出生史 |
| personal_history | string | 否 | 个人史 |
| family_history | string | 否 | 家族史 |
| marital_reproductive_history | string | 否 | 月经婚育史 |
| epidemics_history | string | 否 | 流行病史 |
| look_listen | string | 否 | 望闻切诊 |
| dialectic | string | 否 | 辨证论治 |
| constitution_identification | string | 否 | 体质辨识 |
| wellness_recommendation | string | 否 | 调理建议 |
| doctor_orders | string | 否 | 医嘱 |

**record.diagnose（诊断信息）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| gbs | array | 否 | GBS诊断列表，每项含 name + code + aliases（别名数组） |
| icd10s | array | 否 | ICD10诊断列表，每项含 name + code |
| syndrome | object | 否 | 证型，含 name + code + aliases（别名数组） |
| syndromes | array | 否 | 证型列表，每项含 name + code + aliases（别名数组） |
| therapy | object | 否 | 治法，含 name + code + aliases（别名数组） |
| therapies | array | 否 | 治法列表，每项含 name + code + aliases（别名数组） |

**record.tcm_prescription（中药处方）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | integer | 否 | 处方ID |
| uuid | string | 否 | 处方UUID |
| prescription_reference_name | string | 否 | 处方参考名称 |
| prescription_amount | string | 否 | 处方金额 |
| prescription_approach | string | 否 | 处方方式 |
| taking_method | string | 否 | 服用方法 |
| use_times | string | 否 | 使用次数 |
| processing_method_desc | string | 否 | 加工方法描述 |
| decoction_capacity | string | 否 | 煎药容量 |
| preparation_method | string | 否 | 制备方法 |
| dosage_per_day | string | 否 | 每日剂量 |
| tcm | array | 否 | 中药明细列表（见下表） |

**tcm_prescription.tcm（中药明细）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| sku | string | 否 | SKU |
| medicine_code | string | 否 | 药品编码 |
| pharmacy_short | string | 否 | 药房简称 |
| spu_name | string | 否 | 药品名称 |
| type | string | 否 | 类型 |
| pharmacy_quantity | string | 否 | 药房数量 |
| pharmacy_unit | string | 否 | 药房单位 |
| grams_of_corresponding_pieces | string | 否 | 对应饮片克数 |
| pharmacy_equivalent | string | 否 | 药房等效 |
| take_method_id_desc | string | 否 | 用法描述 |
| his_drug_code | string | 否 | HIS药品编码 |
| his_pharmacy_code | string | 否 | HIS药房编码 |

**record.cpm_prescription（中成药处方）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | integer | 否 | 处方ID |
| cpm | array | 否 | 中成药明细列表（见下表） |

**cpm_prescription.cpm（中成药明细）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| sku | string | 否 | SKU |
| medicine_code | string | 否 | 药品编码 |
| pharmacy_short | string | 否 | 药房简称 |
| spu_name | string | 否 | 药品名称 |
| type | string | 否 | 类型 |
| pharmacy_quantity | string | 否 | 药房数量 |
| pharmacy_unit | string | 否 | 药房单位 |
| take_dosage | string | 否 | 服用剂量 |
| take_unit | string | 否 | 服用单位 |
| daily_taking_times | string | 否 | 每日服用次数 |
| medical_days | string | 否 | 用药天数 |
| medicine_frequency | string | 否 | 用药频率 |
| dosage_route | string | 否 | 给药途径 |
| price | string | 否 | 价格 |
| his_drug_code | string | 否 | HIS药品编码 |
| his_pharmacy_code | string | 否 | HIS药房编码 |

**record.treatment（治疗信息，适宜技术）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| group_id | integer | 否 | 治疗分组ID |
| treat_items | array | 否 | 治疗项列表（见下表） |

**treatment.treat_items（治疗项明细）：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| uuid | string | 否 | 治疗项UUID |
| his_code | string | 否 | HIS治疗项编码 |
| treat_name | string | 否 | 治疗项名称 |
| treat_price | float | 否 | 治疗项价格 |
| treat_content | string | 否 | 治疗内容 |
| treat_count | integer | 否 | 治疗数量 |
| unit | string | 否 | 治疗单位 |

### 3.5 响应字段

**标准对接响应（平台封装）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | 平台返回码（20000=成功） |
| success | boolean | 是否成功 |
| msg | string | 提示信息 |
| data | object | 业务数据容器 |
| data.code | integer | HIS返回码（0=成功） |
| data.message | string | HIS提示信息 |
| data.his_record_id | string | HIS病历ID |
| data.receive_time | string | 接收时间 |
| reason | string | 失败原因（成功时为null） |
| log_id | string | 平台日志ID |

**自定义对接响应（HIS原始格式）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | HIS返回码（0=成功） |
| success | boolean | 是否成功 |
| message | string | HIS提示信息 |
| data | object | 业务数据容器 |
| data.his_record_id | string | HIS病历ID |
| data.receive_time | string | 接收时间 |

### 3.6 标准对接示例

**请求（`<HIS接收地址>`由医院提供）：**

```bash
curl -X POST '<HIS接收地址>' \
-H 'Content-Type: application/json' \
-d '{
    "his_record_id": "34997589",
    "institution": {
        "outer_institution_code": "hospitalA"
    },
    "department": {
        "outer_department_code": "nk_02",
        "name": "内科2"
    },
    "doctor": {
        "outer_doctor_code": "doctor001",
        "name": "医生"
    },
    "patient": {
        "his_patient_id": "DD1577603E1D46DBBF84986D6B950DCE",
        "name": "余喆",
        "gender": "女",
        "age": 32,
        "mobile": "15707647692",
        "idcard": "330825199209161824"
    },
    "record": {
        "appointment_date": "2024-09-18",
        "his_appointment_no": "",
        "his_register_no": "0000535164",
        "patient_basic": {
            "main_remark": "上腹部胀满不适头晕半天",
            "input_remark": "上腹部胀满不适头晕半天，余无不适",
            "objective_remark": "神志清，精神软，颈软，无抵抗",
            "assist_check": "无特殊",
            "disease_history": "否认高血压、糖尿病病史",
            "allergy_history": "否认药物过敏史",
            "birth_history": "",
            "personal_history": "无吸烟饮酒史",
            "family_history": "",
            "marital_reproductive_history": "",
            "epidemics_history": "",
            "look_listen": "舌淡红，苔薄白，脉弦",
            "dialectic": "脾胃气虚证，治以健脾和胃",
            "constitution_identification": "",
            "wellness_recommendation": "",
            "doctor_orders": ""
        },
        "diagnose": {
            "icd10s": [
                {"code": "K29.100x001", "name": "急性胃炎"},
                {"code": "A49.809", "name": "幽门螺杆菌感染"}
            ],
            "gbs": [],
            "syndrome": {"code": "", "name": ""},
            "therapy": {"code": "", "name": ""}
        }
    }
}'
```

**响应：**

```json
{
    "code": 20000,
    "success": true,
    "msg": "成功",
    "data": {
        "code": 0,
        "message": "病历同步成功",
        "his_record_id": "34997589",
        "receive_time": "2024-09-18 10:30:00"
    },
    "reason": null,
    "log_id": "a1b2c3d4e5f6..."
}
```

### 3.7 自定义对接示例

> **提示：** 本接口支持自定义对接。如平台已存在类似接口，请勿重复创建，联系平台进行字段适配，参考本节样例。
> 自定义对接时，HIS端字段名使用自身命名规范，平台通过字段映射转换。

**请求（`<HIS接收地址>`由医院提供）：**

```bash
curl -X POST '<HIS接收地址>' \
-H 'Content-Type: application/json' \
-d '{
    "record_id": "34997589",
    "institution": {"outer_institution_code": "hospitalB"},
    "department": {"department_code": "nk_02"},
    "doctor": {"outer_doctor_code": "doctor001", "name": "医生"},
    "patient": {
        "his_patient_id": "DD1577603E1D46DBBF84986D6B950DCE",
        "name": "余喆"
    },
    "record": {
        "appointment_date": "2024-09-18",
        "appointment_no": "",
        "register_no": "0000535164",
        "patient_basic": {
            "main_remark": "上腹部胀满不适头晕半天",
            "input_remark": "上腹部胀满不适头晕半天",
            "objective_remark": "神志清，精神软",
            "assist_check": "无特殊",
            "disease_history": "否认高血压"
        }
    }
}'
```

**响应：**

```json
{
    "code": 0,
    "success": true,
    "message": "病历同步成功",
    "data": {
        "his_record_id": "34997589",
        "receive_time": "2024-09-18 10:30:00"
    }
}
```

### 3.8 字段映射对照表

| 系统字段名（标准） | HIS字段名（自定义） | 说明 |
| --- | --- | --- |
| his_record_id | record_id | 病历唯一编号 |
| department | department_code | 科室信息 |
| his_appointment_no | appointment_no | 就诊单号 |
| his_register_no | register_no | HIS预约号 |

---

## 4. 药品库存查询（drug_stock_query）

### 4.1 请求地址

#### 4.1.1 HIS 端接收地址（由医院提供）

药品库存查询的目标地址为HIS端API地址，由医院提供。

| 对接模式 | HIS 端地址 | 说明 |
| --- | --- | --- |
| 标准对接 | 由医院提供，配置于系统级 api_url_template | 如 https://his.hospital.com/api/drug/stock |
| 自定义对接 | 由医院提供，配置于医院级 api_url | 覆盖系统级模板，指向医院实际 HIS 接口 |

#### 4.1.2 平台触发地址（平台内部调用）

```
POST /api/system/external-api/invoke
```

### 4.2 请求方式

| 属性 | 值 |
| --- | --- |
| HTTP Method | POST（标准）/ GET（部分医院自定义） |
| Content-Type | application/json |
| Authorization | Bearer <token> |
| 请求格式 | JSON |
| 响应格式 | JSON |
| 超时时间 | 30s |
| 重试次数 | 0 |

### 4.3 请求头

| Header | 值 | 必填 | 说明 |
| --- | --- | --- | --- |
| Content-Type | application/json | 是 | 请求体格式 |
| Authorization | Bearer <token> | 是 | 认证令牌 |

### 4.4 请求参数

> 以下为平台发送至HIS接收地址的业务参数；api_code、params等平台内部调用封装参数不在对接范围内。

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| warehouse_code | string | 是 | 药房/药库编码 |
| drug_codes | array | 是 | 药品编码列表 |

### 4.5 响应字段

> 以下为HIS需返回的原始响应内容（平台内部封装层不在此列出）。

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | HIS返回码（200=成功） |
| data | array | 药品库存数据列表 |
| msg | string | HIS提示信息 |

**data药品库存明细字段：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| drugCode | string | 药品编码 |
| drugName | string | 药品名称 |
| specification | string | 规格 |
| unit | string | 单位 |
| manufacturer | string | 厂家 |
| stockQty | string/integer | 库存数量 |
| batchNo | string | 批号 |
| price | string | 价格 |
| equivalent | string | 药效当量 |
| origin | string | 产地 |

### 4.6 标准对接示例

**请求（`<HIS接收地址>`由医院提供）：**

```bash
curl --location '<HIS接收地址>' \
--header 'Content-Type: application/json' \
--data '{
    "warehouse_code": "5010008",
    "drug_codes": [
        "Y000006914",
        "Y000007099"
    ]
}'
```

**响应：**

```json
{
    "code": 200,
    "data": [
        {
            "drugCode": "Y000006914",
            "unit": "瓶",
            "stockQty": 1
        },
        {
            "drugCode": "Y000007099",
            "unit": "盒",
            "stockQty": 451
        }
    ],
    "msg": "操作成功！"
}
```

### 4.7 自定义对接示例（示例医院）

> **提示：** 本接口支持自定义对接。如平台已存在类似接口，请勿重复创建，联系平台进行字段适配，参考本节样例。
> 示例医院使用GET请求，参数通过query传递，需启用request_processor_script转换参数格式。

**请求（`<HIS接收地址>`由医院提供，平台经request_processor_script转换后发送）：**

```bash
curl --location '<HIS接收地址>' \
--header 'Content-Type: application/json' \
--data '{
    "warehouse_code": "5010008",
    "drug_codes": [
        "Y000006914",
        "Y000007099"
    ]
}'
```

> 医院级配置会自动通过 `request_processor_script` 将 `warehouse_code` 转换为 `drugDeptCode`，将 `drug_codes` 数组转换为逗号分隔的 `drugCodeList`，并通过 GET query 参数发送给 HIS。

**响应：**

```json
{
    "code": 200,
    "data": [
        {
            "drugCode": "Y000006914",
            "unit": "瓶",
            "stockQty": 1
        },
        {
            "drugCode": "Y000007099",
            "unit": "盒",
            "stockQty": 451
        }
    ],
    "msg": "操作成功！"
}
```

### 4.8 字段映射对照表

| 系统字段名（标准） | HIS字段名（自定义） | 说明 |
| --- | --- | --- |
| warehouse_code | drugDeptCode | 药房/药库编码 |
| drug_codes | drugCodeList | 药品编码列表（逗号分隔） |

---

## 5. HIS主动查询病历（medical_record_query）

该接口为入站接口（inbound），由 HIS 端主动调用，接口地址由平台提供。HIS 凭已同步的 `his_record_id` 主动查询平台侧已保存的病历完整信息（含诊断、中药/成药处方等）。

> **说明：** 本接口经 external-api 网关入站，已是 POST + JSON body，平台将请求透传至内部端点 `(current_app)/api/integration/external/medical-record`。仅需创建系统级标准配置，无需医院级配置与服务处理器（service_handler）。

### 5.1 请求地址

HIS通过以下地址主动查询病历：

```
POST /api/system/external-api/inbound/<hospital_code>/medical_record_query/
```

### 5.2 请求方式

| 属性 | 值 |
| --- | --- |
| HTTP Method | POST |
| Content-Type | application/json |
| 请求格式 | JSON |
| 响应格式 | JSON |
| 鉴权 | 无需认证令牌（permission_classes=[AllowAny]） |

### 5.3 请求参数

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| app_id | string | 是 | 医院AppId |
| app_secret | string | 是 | 医院AppSecret |
| his_record_id | string | 是 | HIS病历唯一标识 |
| outer_institution_code | string | 是 | 医院外部编码 |

### 5.4 响应字段

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | 平台返回码（20000=成功） |
| success | boolean | 是否成功 |
| msg | string | 提示信息 |
| data | object | 病历数据容器（见下表） |
| reason | string/null | 失败原因（成功时为null） |
| log_id | string | 平台日志ID |

**data（病历数据）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| his_record_id | string | HIS病历唯一标识 |
| institution | object | 机构信息（原始请求数据） |
| department | object | 科室信息（原始请求数据） |
| doctor | object | 医生信息（原始请求数据） |
| patient | object | 患者信息（原始请求数据） |
| record | object | 病历详情（见下表） |

**data.record（病历详情）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| appointment_date | string | 预约日期 |
| his_appointment_no | string | HIS 预约号 |
| his_register_no | string | HIS 挂号号 |
| patient_basic | object | 患者病历基础信息（见下表） |
| diagnose | object | 诊断信息（见下表） |
| tcm_prescription | array | 中药处方列表（见下表） |
| cpm_prescription | array | 成药处方列表（见下表） |
| treatment | array/null | 治疗信息列表（适宜技术，见下表） |
| note | string | 备注 |
| invitation_date | string/null | 下次就诊日期 |

**data.record.patient_basic（患者病历基础信息）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| height | integer | 身高 |
| weight | integer | 体重 |
| temperature | float | 体温 |
| heart_rate | float | 心率 |
| systolic_blood_pressure | float | 收缩压 |
| diastolic_blood_pressure | float | 舒张压 |
| main_remark | string | 主诉 |
| input_remark | string | 现病史 |
| objective_remark | string | 体格检查 |
| assist_check | string | 辅助检查 |
| disease_history | string | 既往史 |
| allergy_history | string | 过敏史 |
| birth_history | string | 出生史 |
| personal_history | string | 个人史 |
| family_history | string | 家族史 |
| marital_reproductive_history | string | 月经婚育史 |
| epidemics_history | string | 流行病史 |

> 病情类字段（主诉至医嘱）从病历字段内容（field_contents）解析，无内容时不返回对应键。

**data.record.diagnose（诊断信息）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| gbs | array | 中医诊断列表，每项含 name + code + aliases（别名数组） |
| icd10s | array | 西医诊断列表，每项含 name + code |
| syndrome | object | 证型，含 name + code + aliases（别名数组） |
| syndromes | array | 证型列表，每项含 name + code + aliases（别名数组） |
| therapy | object | 治法，含 name + code + aliases（别名数组） |
| therapies | array | 治法列表，每项含 name + code + aliases（别名数组） |

> 诊断条目从病历字段内容（gb/icd/syndrome/therapy）解析并匹配知识库标准数据，未命中的条目不返回。
> 成功判定规则：`$.code eq 20000`

### 5.5 请求示例

**请求：**

```bash
curl --location 'localhost:9100/api/system/external-api/inbound/hospitalA/medical_record_query/' \
--header 'Content-Type: application/json' \
--data '{
    "app_id": "b673ced0db0faf493649fe7c6c4b41c",
    "app_secret": "X5bYMxyK3neJ-1f25sSgKUGUqIM8Usr9uY1Zi0Wg",
    "his_record_id": "34997589",
    "outer_institution_code": "hospitalA"
}'
```

**响应：**

```json
{
    "code": 20000,
    "success": true,
    "msg": "获取病历成功",
    "data": {
        "his_record_id": "34997589",
        "institution": {
            "outer_institution_code": "hospitalA"
        },
        "department": {
            "outer_department_code": "nk_02",
            "name": "内科2"
        },
        "doctor": {
            "outer_doctor_code": "doctor001",
            "name": "医生"
        },
        "patient": {
            "his_patient_id": "DD1577603E1D46DBBF84986D6B950DCE",
            "name": "余喆"
        },
        "record": {
            "appointment_date": "2024-09-18",
            "his_appointment_no": "",
            "his_register_no": "0000535164",
            "patient_basic": {
                "main_remark": "上腹部胀满不适头晕半天",
                "input_remark": "上腹部胀满不适头晕半天，余无不适",
                "objective_remark": "神志清，精神软",
                "assist_check": "无特殊",
                "disease_history": "否认高血压、糖尿病病史",
                "allergy_history": "否认药物过敏史",
                "personal_history": "无吸烟饮酒史",
                "look_listen": "舌淡红，苔薄白，脉弦",
                "dialectic": "脾胃气虚证，治以健脾和胃",
                "temperature": 36.5,
                "heart_rate": 80
            },
            "diagnose": {
                "gbs": [],
                "icd10s": [
                    {"code": "K29.100x001", "name": "急性胃炎"}
                ],
                "syndrome": {"code": "", "name": ""},
                "therapy": {"code": "", "name": ""}
            },
            "tcm_prescription": [
                {
                    "id": 1,
                    "prescription_reference_name": "桂枝汤",
                    "prescription_amount": 1,
                    "tcm": [
                        {
                            "medicine_code": "MED001",
                            "pharmacy_short": "桂枝",
                            "type": "饮片",
                            "pharmacy_quantity": 9,
                            "pharmacy_unit": "g",
                            "take_method_id_desc": "先煎"
                        }
                    ]
                }
            ],
            "cpm_prescription": [
                {
                    "id": 2,
                    "cpm": [
                        {
                            "medicine_code": "MED002",
                            "pharmacy_short": "感冒清热颗粒",
                            "take_dosage": 1,
                            "take_unit": "袋",
                            "daily_taking_times": 3,
                            "medical_days": 3
                        }
                    ]
                }
            ],
            "treatment": [
                {
                    "group_id": 1001,
                    "treat_items": [
                        {
                            "uuid": "a1b2c3d4e5f6",
                            "his_code": "ZL001",
                            "treat_name": "针刺",
                            "treat_price": 30.0,
                            "treat_content": "普通针刺治疗",
                            "treat_count": 1
                        }
                    ]
                }
            ],
            "note": "",
            "invitation_date": null
        }
    },
    "reason": null,
    "log_id": "f8ae9faadcd442f78ce36de6ebef4e2b"
}
```

**data.record.tcm_prescription（中药处方）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | integer | 处方ID |
| uuid | string | 处方UUID |
| prescription_reference_name | string | 处方参考名称 |
| prescription_amount | integer | 处方金额 |
| prescription_approach | string | 处方方式 |
| taking_method | string | 服用方法 |
| use_times | string | 使用次数 |
| processing_method_desc | string | 加工方法描述 |
| decoction_capacity | string/null | 煎药容量 |
| preparation_method | string | 制备方法 |
| dosage_per_day | string | 每日剂量 |
| tcm | array | 中药明细列表（见下表） |

**tcm_prescription.tcm（中药明细）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| sku | string | SKU |
| medicine_code | string | 药品编码 |
| pharmacy_short | string | 药品简称 |
| spu_name | string | 药品名称 |
| type | string | 类型 |
| pharmacy_quantity | float | 用量（克） |
| pharmacy_unit | string | 单位 |
| grams_of_corresponding_pieces | float | 对应饮片克数 |
| pharmacy_equivalent | float/null | 药房等效 |
| take_method_id_desc | string | 用法描述 |
| his_drug_code | string/null | HIS药品编码 |
| his_pharmacy_code | string/null | HIS药房编码 |

**data.record.cpm_prescription（成药处方）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | integer | 处方ID |
| cpm | array | 成药明细列表（见下表） |

**cpm_prescription.cpm（成药明细）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| sku | string | SKU |
| medicine_code | string | 药品编码 |
| pharmacy_short | string | 药品简称 |
| spu_name | string | 药品名称 |
| type | string | 类型 |
| pharmacy_quantity | float | 用量 |
| pharmacy_unit | string | 单位 |
| take_dosage | string/null | 服用剂量 |
| take_unit | string/null | 服用单位 |
| daily_taking_times | integer/null | 每日服用次数 |
| medical_days | integer/null | 用药天数 |
| medicine_frequency | string/null | 用药频率 |
| dosage_route | string/null | 给药途径 |
| price | float/null | 价格 |
| his_drug_code | string/null | HIS药品编码 |
| his_pharmacy_code | string/null | HIS药房编码 |

**data.record.treatment（治疗信息，适宜技术）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| group_id | integer | 治疗分组ID |
| treat_items | array | 治疗项列表（见下表） |

**treatment.treat_items（治疗项明细）：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| uid | string | 治疗项UUID |
| his_code | string | HIS治疗项编码 |
| treat_name | string | 治疗项名称 |
| treat_price | float | 治疗项价格 |
| treat_content | string | 治疗内容 |
| treat_count | integer | 治疗数量 |
| unit | string | 治疗单位 |

### 5.6 错误场景

| 场景 | msg 提示 |
| --- | --- |
| 基础参数缺失 | 参数校验失败提示（如 app_id 不能为空） |
| 医院配置无效 | 医院配置校验失败 |
| 未找到病历 | 病历不存在 |
| 未找到原始请求数据 | 未找到对应的请求数据 |

---

## 6. 外部诊断信息查询（outer_diagnoses_query）

出站接口（outbound）：前端携查询参数请求平台 → 平台按api_code加载接口配置 → 自动签名（HMAC-SHA256规范化请求签名） → 调用外部平台「查询诊断数据接口」`POST /open/api/v1/diagnoses` → 响应透传回前端。

四诊（望/闻/切）信息即诊断记录中的设备测量数据（面/舌/舌下/口气/脉）。

### 6.1 请求地址

#### 6.1.1 前端调用地址（推荐）

```
POST /api/integration/external/diagnosis
```

> 服务端内部固定以 `outer_diagnoses_query` 委托通用出站服务。

#### 6.1.2 外部平台接收地址（由外部提供）

| 对接模式 | 外部地址 | 说明 |
| --- | --- | --- |
| 标准对接 | 配置于系统级 api_url_template | 如 https://open.macrocuria.cn/open/api/v1/diagnoses（占位，以实际分配为准） |
| 自定义对接 | 配置于医院级 api_url | 覆盖系统级模板 |

### 6.2 请求方式

| 属性 | 值 |
| --- | --- |
| HTTP Method | POST |
| Content-Type | application/json |
| 请求格式 | JSON |
| 响应格式 | JSON |
| 超时时间 | 30s |
| 重试次数 | 0 |
| 认证 | 平台自动注入签名头（AppKey/AppSecret由医院级配置提供） |

### 6.3 请求参数（前端 → 平台）

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| idCardNumber | string | 二选一 | 患者身份证号（与 phoneNumber 至少传一项） |
| phoneNumber | string | 二选一 | 患者手机号（与 idCardNumber 至少传一项） |
| startTime | string | 是 | 创建时间下限，ISO 8601，如 2026-07-29T00:00:00+08:00 |
| endTime | string | 是 | 创建时间上限，ISO 8601，如 2026-07-29T23:59:59+08:00 |

> 平台向外部平台发送的请求体仅包含 idCardNumber/phoneNumber/startTime/endTime，同时自动注入 X-App-Key/X-Timestamp/X-Nonce/X-Signature 四个签名头，前端无需关心。

### 6.4 响应字段（平台 → 前端，标准封装）

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| code | integer | 平台返回码（20000=成功） |
| success | boolean | 是否成功 |
| msg | string | 提示信息 |
| data | array | 诊断记录列表（外部平台 data 原样透传，可能为空数组） |
| reason | string/null | 失败原因（成功时为null） |
| log_id | string | 平台日志ID |

**data诊断记录字段：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 诊断记录ID（36位） |
| idCardNumber | string | 身份证号 |
| phoneNumber | string | 手机号 |
| createTime | string | 诊断记录创建时间 |
| measurements | array | 设备测量数组（见下表） |

**measurements设备测量字段：**

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| type | integer | 测量类型（1=面/2=舌/3=舌下/4=脉/5=口气等，以实际为准） |
| createTime | string | 测量时间 |
| image | string | 图片地址（type为1/2/3时可能返回） |
| pulse | array | 脉象数据（type为4时返回），含 position/values/labels |

### 6.5 请求示例

**请求（前端调用专用接口）：**

```bash
curl --location 'localhost:9100/api/integration/external/diagnosis' \
--header 'Content-Type: application/json' \
--data '{
    "idCardNumber": "110101199001011234",
    "startTime": "2026-07-29T00:00:00+08:00",
    "endTime": "2026-07-29T23:59:59+08:00"
}'
```

**响应：**

```json
{
    "code": 20000,
    "success": true,
    "msg": "成功",
    "data": [
        {
            "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "idCardNumber": "110101199001011234",
            "phoneNumber": "13800000000",
            "createTime": "2026-07-29T10:30:00+08:00",
            "measurements": [
                {
                    "type": 1,
                    "createTime": "2026-07-29T10:30:05+08:00",
                    "image": "https://example.com/tmp/tongue.jpg"
                },
                {
                    "type": 4,
                    "createTime": "2026-07-29T10:31:00+08:00",
                    "pulse": [
                        {
                            "position": "cun",
                            "values": [72, 75, 78],
                            "labels": ["滑脉"]
                        }
                    ]
                }
            ]
        }
    ],
    "reason": null,
    "log_id": "f8ae9faadcd442f78ce36de6ebef4e2b"
}
```

### 6.6 关键约束

- `idCardNumber` / `phoneNumber` 二选一：平台服务层前置校验，至少传一项，缺失直接返回错误（不外呼）；
- 签名自动完成：开启医院级 `auth_enabled` 并关联 `puyin_hmac_sha256`（例）（签名算法）认证方案后，出站自动注入签名头；
- 凭证配置：当前默认走普茵设备接口，AppKey...
- Nonce 防重放：平台每次请求自动生成新 UUID nonce，外部平台在 5 分钟内拒绝重复 AppKey+Nonce。

---

## 7. 错误码说明

### 7.1 平台响应码（标准对接）

| code | 说明 |
| --- | --- |
| 20000 | 成功 |
| 90020 | 业务处理失败 |
| 90021 | 接口调用失败 |
| 90022 | 数据解析异常 |
| 90023 | 结果校验失败（success rule 不匹配） |

### 7.2 HIS返回码（例）

| code | 说明 |
| --- | --- |
| 0 | 成功 |
| 非0 | 失败（具体含义由HIS定义） |

---

## 8. 对接说明

| 维度 | 标准对接 | 自定义对接 |
| --- | --- | --- |
| HIS端接收地址 | 医院提供（配置于 api_url_template） | 医院提供（配置于 api_url） |
| 请求字段命名 | snake_case (warehouse_code) | HIS端命名（drugDeptCode） |
| 成功判定 | $code eq 200 | $code eq 200 |
| 响应格式 | 平台封装（含 log_id） | HIS原始格式 |
| 字段映射 | 无 | system_field → external_field |
| 平台触发地址 | /api/system/external-api/invoke | /api/system/external-api/hospital/{id}/invoke |
| 请求处理器 | 无 | 可启用 request_processor_script 转换参数 |

> **字段命名说明：** 请求字段命名由对接双方自行约定，上表 `warehouse_code` / `drugDeptCode` 等仅为示例，并非固定写法。

---

**配套配置手册：** `辅助诊疗系统_接口配置手册.md`